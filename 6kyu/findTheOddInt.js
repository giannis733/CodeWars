// Find the Odd Int
// https://www.codewars.com/kata/54da5a58ea159efa38000836

const findOdd = (A) => {
  for (number of A ){
    if ((A.filter(n => n === number)).length % 2 === 1) return number;
  }
};